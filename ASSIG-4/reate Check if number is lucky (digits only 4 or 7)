def is_lucky_number(num):
    for ch in str(num):
        if ch not in ('4', '7'):
            return False
    return True

# Example usage
print(is_lucky_number(4747))  # True
print(is_lucky_number(123))   # False
